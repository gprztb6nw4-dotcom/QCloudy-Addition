# Alpha 2.8.19——完整性与性能维护

- 缓存兼容性缺失报告的提供方分组与换行行布局，不再每帧重复构建。
- 将有效的 Lasso `REEL` 检测缩减为一遍已加载实体遍历。
- 删除未使用的 Shard 全配方重复引用列表，不改变任何配方结果。
- 消除项目脚本中的 Gradle 10 执行期 API 弃用警告。
- 明确从可运行 JAR 与 Sources JAR 中排除 Finder `.DS_Store` 元数据。
- 功能默认值、提供方写入、命令、网络行为和纯客户端安全边界均未改变。
- 分别为 Minecraft 26.1.2 与 26.2 构建。
