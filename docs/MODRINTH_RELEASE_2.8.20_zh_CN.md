# Alpha 2.8.20——可视化按需提供方扫描

- 开启“统一设置编辑”或“统一 HUD 编辑”时显示实时扫描进度页。
- 两个独立编辑页面都加入 Refresh。
- 共用一份经过校验的会话快照，但设置/HUD 数量分别显示。
- 不显示未安装模组，只列出已安装且成功读取能力的提供方。
- 本地确定性分类器只处理仍未分类的元数据。
- 取消普通打开设置菜单时的无条件重复扫描。
- 扫描完全在本地只读进行：没有云端 AI、网络/服务器请求、数据包、命令、遥测、自动化或直接编辑配置文件。
