# Alpha 2.8.18——兼容性缺失报告

- 在“通用 → 兼容模组”中加入没有开关的只读“兼容性缺失报告”。
- 按模组分组显示已明确识别但无法统一管理的内容，并标记“设置/HUD 编辑”。
- 正常支持的功能不会显示，也不会给完全未知的未来结构编造名称。
- 检查不写入、不调用保存、不与服务器通信、不发 HTTP 请求，也不进行自动化。
- 分别提供 Minecraft 26.1.2 与 26.2 构建。
