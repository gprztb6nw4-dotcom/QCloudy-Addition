## Alpha 2.8.26

- 删除不完整的 Flare 聊天假设，为 Warning、Alert、SOS Flare 改用确认后的本地三分钟生命周期。
- 只有精确使用 Flare 道具并收到匹配的成功放置音效后才会启动计时；放置失败或冷却中误点不会制造假提醒。
- 四种 Power Orb 继续严格匹配玩家本人收到的消失聊天。
- 加入 Power Orb、Flare、中央大字、提示音和 0–100% 音量独立设置；音量默认 64%。
- 删除距离、增益范围和实体卸载推断。换世界/服务器时静默清理，同一次生命周期结束最多提醒一次。

仅面向 Minecraft 26.1.2 的纯客户端 Alpha；不会发送聊天、命令、数据包、交互或网络请求。
