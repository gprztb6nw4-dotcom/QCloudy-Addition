# Alpha 2.8.21——Feesh 统一管理

- 将 Feesh 作为第五个可选 QCA 统一设置/HUD 提供方。
- 按能力探测实时设置，通过 Feesh 公开 setter 写入，并由 Feesh 自身保存。
- 将已启用且有内容的 Feesh Overlay 加入“编辑 HUD”，正确换算对齐锚点并原生持久化。
- 不支持的 Feesh 设置/HUD 进入“兼容性缺失报告”，不会猜测，也不会隐藏正常同级功能。
- 两个集成总开关继续保持独立、按需启用且默认关闭。
- 分别构建 Minecraft 26.1.2 与 26.2 文件；Feesh 仍为可选模组。
