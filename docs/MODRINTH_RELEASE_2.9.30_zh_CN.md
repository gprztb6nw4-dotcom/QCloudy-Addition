Alpha 2.9.30 修复 SOS/Flare 重新放置后的计时问题。

- 重新放置 Warning、Alert 或 SOS Flare 会立即重新开始完整三分钟生命周期，包括不重复首次放置确认信号的替换。
- 旧到期时间会失效，不会在新 Flare 仍生效时提前提醒。
- 加入对方块使用追踪，并在使用回调漏失但精确放置音效和手持 Flare 仍匹配时安全恢复确认。
- 加入重复放置 SOS 与防止错误重置的回归测试。
- 提供 Minecraft 26.1.2 版本。

QCA 仍可独立运行且为纯客户端模组。只安装可运行 JAR；`-sources.jar` 仅供开发者使用。
