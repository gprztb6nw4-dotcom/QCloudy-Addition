# Alpha 2.8.22——每次提供方扫描均需确认

- 首次执行统一设置/HUD 扫描，以及每一次 Refresh 前，都会显示第二次确认。
- 取消首次确认会保持总开关关闭；取消 Refresh 会保留上一份有效快照。
- 重启后恢复为开启的开关不再静默扫描。
- 已有扫描运行时 Refresh 不可点击。
- 扫描仍为本地、确定性、只读过程，不请求服务器或网络，也不执行自动输入。
- 分别提供 Minecraft 26.1.2 与 26.2 的 Fabric 构建。
